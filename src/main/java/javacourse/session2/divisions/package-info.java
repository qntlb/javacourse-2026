/**
 * In this package we introduce static methods through a simple calculator example. The class
 * Calculator has both a static method, which takes its arguments explicitly and does not
 * depend on any field of the class, and a non-static method, which uses the field of the
 * object calling it. This contrast illustrates when it is appropriate to declare a method
 * as static and when it must be an instance method.
 */
package javacourse.session2.divisions;
