/**
 * In this package we contrast static and non-static members of a class. The class
 * StaticExample has both a static field and a non-static field, and both a static method
 * and a non-static method. A key rule is illustrated: a static method cannot reference
 * non-static fields or call non-static methods of the same class, because static members
 * belong to the class itself and are not tied to any particular object.
 */
package javacourse.session2.staticexample;
