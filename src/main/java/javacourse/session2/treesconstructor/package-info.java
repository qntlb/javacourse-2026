/**
 * In this package we illustrate constructor overloading through a Tree class that has both a
 * parameterized constructor taking the height of the tree as a double argument, and a default
 * constructor with no arguments. We also observe that primitive fields of a class are
 * automatically initialized to zero even when no explicit value is assigned in the constructor,
 * which is a guarantee provided by Java for class fields but not for local variables.
 */
package javacourse.session2.treesconstructor;
