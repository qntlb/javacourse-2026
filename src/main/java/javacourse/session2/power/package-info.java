/**
 * In this package we illustrate method overloading for different input types through a power
 * computation example. The method computePower is overloaded with an int exponent version and
 * a double exponent version: when the exponent is a double and the base is negative, the result
 * would be a complex number, so the double version warns the user. This shows how overloading
 * allows different handling strategies depending on the type of the inputs.
 */
package javacourse.session2.power;
