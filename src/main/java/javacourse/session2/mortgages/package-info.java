/**
 * In this package we illustrate static fields through a mortgage example. The class Mortgage
 * has a static field loansSum that tracks the total amount of money lent across all objects
 * of the class: since it is static, it is shared by all instances and gets incremented
 * every time a new Mortgage object is created. This shows how static fields are stored in a
 * single shared location and are useful for keeping information that belongs to the class
 * as a whole rather than to any individual object.
 */
package javacourse.session2.mortgages;
