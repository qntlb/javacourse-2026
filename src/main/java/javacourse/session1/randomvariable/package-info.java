/**
 * In this package we illustrate the do...while loop through a coin-flipping simulation:
 * the loop body is executed at least once before the condition is checked, which is the
 * key distinction between do...while and while. We also introduce the Random class from
 * java.util and its nextInt(n) method, which generates pseudo-random integers between
 * 0 (inclusive) and n (exclusive).
 */
package javacourse.session1.randomvariable;
