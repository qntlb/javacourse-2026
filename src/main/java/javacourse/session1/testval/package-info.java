/**
 * In this package we check whether a number lies inside a given interval, using several
 * equivalent implementations: an if/else statement with logical operators, a compact
 * boolean expression, a version returning an integer instead of a boolean, and a version
 * using the ternary operator. The package illustrates how the same logic can be expressed
 * in different ways, and introduces the ternary operator as a concise alternative to if/else.
 */
package javacourse.session1.testval;
