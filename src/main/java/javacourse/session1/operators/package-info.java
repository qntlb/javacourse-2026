/**
 * In this package we illustrate the main categories of operators in Java: arithmetic operators
 * (+, -, *, /, %), relational operators (==, !=, <, >, <=, >=), logical operators (&, ||, !),
 * and increment/decrement operators (++ and --). We also introduce primitive types such as
 * int, double, boolean, char and String, and we show the concepts of upcasting (treating an
 * int as a double to avoid truncation) and downcasting (converting a double back to an int).
 */
package javacourse.session1.operators;
