/**
 * In this package we illustrate the if/else statement through a simple divisibility check:
 * a number is divided by another using the modulo operator %, which returns the remainder
 * of the integer division, and the result is tested with a boolean expression to determine
 * whether the first number is divisible by the second.
 */
package javacourse.session1.divisible;
