/**
 * In this package we illustrate the difference between primitive values and object references
 * in Java memory. When two reference variables point to the same object (aliasing), modifying
 * the object through one reference also changes what is seen through the other. This is
 * demonstrated with two Tank objects and contrasted with the behavior of primitives, where
 * assignment copies the value rather than the reference.
 */
package javacourse.session1.tank;
