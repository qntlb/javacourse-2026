/**
 * In this package we introduce object-oriented programming through a first example: the class
 * Message has one field and two methods, illustrating the interplay between state and behavior
 * in a class. We see how to construct objects with the new keyword, how to access fields and
 * call methods on an object, and how the output of a method can depend on the state of the
 * specific object calling it.
 */
package javacourse.session1.oophelloworld;
