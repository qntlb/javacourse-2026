/**
 * In this package we show two ways in which protected members can be accessed: from within
 * the same package, and from a derived class in another package. The class ProtectedSamePackage
 * has a protected field and a protected method; the class DerivedClass in this same package can
 * access them because protected implies package access. Together with the protectedotherpackage
 * package, this illustrates the full scope of the protected access modifier.
 */
package javacourse.session4.testingprotection;
