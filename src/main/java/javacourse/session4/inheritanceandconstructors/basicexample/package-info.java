/**
 * In this package we lay the foundation of inheritance and constructor chaining: when an object
 * of a derived class is created, the constructor of the parent class is automatically called
 * first, even if it is not explicitly invoked with super(). The package shows the syntax of
 * the extends keyword and illustrates through the printed output that constructing a DerivedClass
 * object triggers the ParentClass constructor before the body of the derived constructor executes.
 */
package javacourse.session4.inheritanceandconstructors.basicexample;
