/**
 * In this package we introduce inheritance with a first example: the class Smartphone extends
 * the base class Phone using the extends keyword, and thereby inherits its fields and methods
 * without having to rewrite them. We also see a first example of method overriding: the call()
 * method of Phone is re-implemented in Smartphone with additional behavior, using the @Override
 * annotation and the super keyword to invoke the parent class implementation.
 */
package javacourse.session4.inheritance.phones;
