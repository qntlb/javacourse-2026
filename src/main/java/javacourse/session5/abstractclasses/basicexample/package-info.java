/**
 * This package illustrates a first basic example of abstract classes. Note that we can
 * create objects of classes inheriting from the abstract class, but not directly instances of the abstract class itself.
 * Indeed, they could not call the (not implemented!) abstract method. However, in the class OtherClass you can see
 * that thanks to late binding you can define a method whose argument is AbstractBaseClass:
 * this is another example of polymorphism.
 *
 * 
 * 
 */
package javacourse.session5.abstractclasses.basicexample;

