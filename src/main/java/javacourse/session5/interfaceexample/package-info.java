/**
 * In this package we illustrate interfaces through a first example: this interface is a "completely abstract"
 * class, as it only gives the return type, name and arguments lists of the methods that a class
 * "implementing" that interface must implement, and a final, static field. It provides the
 * "form" of a class implementing this interface, but not the implementation.
 */
package javacourse.session5.interfaceexample;