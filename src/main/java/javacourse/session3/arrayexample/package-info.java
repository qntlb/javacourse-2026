/**
 * In this package we introduce arrays in Java: fixed-size sequences of elements of the same
 * type, indexed starting from zero. We see how to declare and initialize an array with the
 * new keyword, how to access and modify elements, how arrays behave as object references
 * (so that assigning one array to another creates an alias), and how to avoid aliasing using
 * the clone() method. We also use utility methods from java.util.Arrays, such as toString()
 * for printing and sort() for sorting.
 */
package javacourse.session3.arrayexample;
