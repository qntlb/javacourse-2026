/**
 * In this package we define two public utility classes that are reused throughout the course.
 * The class Print provides static methods to print objects with or without a trailing newline,
 * serving as a convenient shorthand for System.out.println() and System.out.print(). The class
 * Vector is a minimal stub used to illustrate naming conflicts when two imported packages
 * contain a class with the same name, as shown in javacourse.session3.packageimporting.
 */
package javacourse.session3.useful;
