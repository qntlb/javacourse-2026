/**
 * In this package we illustrate how to import other packages in Java. We show that using the
 * wildcard import syntax (import package.*) brings all classes of a package into scope, and
 * that import static allows calling static methods without specifying the class name every time.
 * We also show what happens when two imported packages contain a class with the same name:
 * in that case the compiler cannot resolve the ambiguity and one must use the fully qualified
 * class name to disambiguate.
 */
package javacourse.session3.packageimporting;
