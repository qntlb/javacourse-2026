/**
 * In this package we show how to use a public utility class imported from another package.
 * The class SimplerPrinterExample imports the class Print from javacourse.session3.useful
 * and calls its static methods printn() and print() to produce output. We also illustrate
 * the import static declaration, which allows calling static methods of a class without
 * writing the class name each time.
 */
package javacourse.session3.simplerprinterexample;
