/**
 * In this package we illustrate all four access modifiers available in Java: public, which
 * allows access from everywhere; package access (no modifier), which restricts access to
 * classes in the same package; private, which restricts access to within the class itself;
 * and protected, which will be introduced in session 4. Attempting to access package-access
 * or private members from outside the package, as shown in the otherpackage package, results
 * in a compilation error.
 */
package javacourse.session3.accesslevels;
